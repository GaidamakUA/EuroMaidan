<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.com.studiovision.euromaidan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="ua.com.studiovision.euromaidan.LoginActivity_"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name="ua.com.studiovision.euromaidan.RegisterActivity_"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name="ua.com.studiovision.euromaidan.FirstRunActivity_"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name="ua.com.studiovision.euromaidan.FeedActivity_"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MainService_"
            android:enabled="true"
            android:exported="false"></service>
        <provider
                android:name="ua.com.studiovision.euromaidan.provider.EmContentProvider"
                android:authorities="ua.com.studiovision.euromaidan.provider"
                android:exported="false" />
    </application>

</manifest>
